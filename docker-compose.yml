version: "3.9"

services:
  # Central FastAPI coordination gateway
  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: precis

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    restart: unless-stopped

    stdin_open: true

    tty: true